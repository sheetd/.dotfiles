[user]
  name = sheetd
  email = sheetd@users.noreply.github.com
[color]
  ui = auto
[core]
  editor = nano
[pull]
  rebase = false
[filter "lfs"]
  smudge = git-lfs smudge -- %f
  process = git-lfs filter-process
  required = true
  clean = git-lfs clean -- %f