- defaults:
    link:
        relink: true

- clean: ["~"]

- link:
    ~/.aws: shell/.aws
    ~/.ssh: shell/.ssh
    ~/.zshrc: shell/.zshrc
    # ~/.oh-my-zsh: shell/ohmyzsh/

- create:
    - ~/github
